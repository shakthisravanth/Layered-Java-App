// Create a class named CurrentAccount inside the bank package

// Make CurrentAccount inherit from the Account class using 'extends'

// Create a constructor that accepts:
// accountNumber, holderName, and balance

// Inside the constructor:
// Call the parent class constructor using super(accountNumber, holderName, balance)


// Note for students:
// This class does not override any methods
// It uses the same deposit() and withdraw() behavior from the parent Account class

