run-button: >
    find BankProject -name "*.java" > sources.txt &&
    javac @sources.txt &&
    java -cp BankProject app.Main

terminals:
    - find BankProject -name "*.java" > sources.txt && javac @sources.txt && java -cp BankProject app.Main

playground-view: terminal-editor
