run-button: >
  cd /home/damner/code/BankProject &&
  javac model/*.java repository/*.java service/*.java ui/*.java app/*.java &&
  java -cp . app.Main

terminals:
  - cd /home/damner/code/BankProject && javac model/*.java repository/*.java service/*.java ui/*.java app/*.java && java -cp . app.Main

playground-view: terminal-editor
